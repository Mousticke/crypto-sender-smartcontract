{
  "name": "crypto-sender-smartcontract",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "SET NODE_OPTIONS=--openssl-legacy-provider && npx hardhat test",
    "deploy_ropsten": "SET NODE_OPTIONS=--openssl-legacy-provider && npx hardhat run scripts/deploy.js --network ropsten",
    "node": "SET NODE_OPTIONS=--openssl-legacy-provider && npx hardhat node"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.3",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "chai": "^4.3.4",
    "dotenv": "^10.0.0",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.5.2",
    "hardhat": "^2.8.0"
  }
}
